// import knex from '../knex';
// import { ORDER_TABLE } from './tables';

export class RewardsService {


    public createOrder = async (cardId: string) => {
        // const userInfo = await (knex.select('point', 'id', 'progress')
        //     .from(ORDER_TABLE)
        //     .where({ id }));

        // return userInfo;
    }



}

